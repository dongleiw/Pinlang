函数是代码逻辑的基本单元. 方法是稍微特殊一点的函数

函数从地址确定的阶段分为两种:
	一种是编译阶段(解释型语言的检查阶段)固定的, 这类函数的地址是执行之前就固定的.
		比如普通的函数调用, 泛型函数调用
	另一种是运行时确定的
		比如通过函数指针调用. 函数指针是一个变量, 内容可变, 因此无法在编译阶段确定.
		另外动态库引入的函数, 地址也是动态的


main函数
	作为函数的入口
